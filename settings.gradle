pluginManagement {
    // Include 'plugins build' to define convention plugins.
    includeBuild('build-logic')
}

rootProject.name = 'ethelred_games'
include('client', 'server', 'engine', ':games:nuo', ':games:duo', 'auto-player')
